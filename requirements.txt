# UPeU Internado 360 — Phase 1 / Part 1
# Backend dependencies. Pinned to compatible ranges for a reproducible prototype.

fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
Jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-dotenv==1.0.1
email-validator==2.2.0

# Batch 2E — Excel export (openpyxl) and local PDF generation (fpdf2, pure-Python;
# pulls in Pillow + fonttools). No paid services, fully offline.
openpyxl==3.1.5
fpdf2==2.8.7

# AI Coordinator Assistant (Phase 3A/3B) — used only to *summarize*
# deterministic query results (never to query the database directly).
# Optional at runtime: the assistant's deterministic fallback works even if
# neither package is installed, or the selected provider's API key is unset
# (both are lazy-imported and guarded). Install whichever provider you use
# via AI_ASSISTANT_PROVIDER; both may be installed side by side.
anthropic==0.40.0
google-genai==1.2.0

# Testing
pytest==8.3.4
httpx==0.28.1

# UPeU Internado 360 — Phase 1 / Part 1
# Backend dependencies. Pinned to compatible ranges for a reproducible prototype.

fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
Jinja2==3.1.5
python-multipart==0.0.20
itsdangerous==2.2.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-dotenv==1.0.1
email-validator==2.2.0

# Batch 2E — Excel export (openpyxl) and local PDF generation (fpdf2, pure-Python;
# pulls in Pillow + fonttools). No paid services, fully offline.
openpyxl==3.1.5
fpdf2==2.8.7

# Testing
pytest==8.3.4
httpx==0.28.1

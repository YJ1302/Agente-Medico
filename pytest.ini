[pytest]
# Tests run against a temporary SQLite database configured in tests/conftest.py,
# never the demo database (requirement N).
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
filterwarnings =
    ignore::DeprecationWarning

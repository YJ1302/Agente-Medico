"""Service layer — business logic and orchestration.

Services depend on repositories (data access) and agents (automation). Routes
depend on services. Business logic must never live in templates or routes.
"""

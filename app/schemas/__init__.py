"""Pydantic schemas package (reserved).

Part 1 renders server-side HTML and does not expose a JSON API surface, so no
request/response schemas are required yet. Pydantic schemas for API endpoints
and form validation will be added here in Parts 2 & 3, keeping validation
models separate from ORM models.
"""
